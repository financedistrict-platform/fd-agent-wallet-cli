# Copy this file to .env and fill in values for your environment.
# .env is gitignored — never commit actual URLs or secrets.

# Override Entra auth settings (production defaults are built-in)
# FDX_AUTHORITY=https://auth.fd.xyz/financedistrict.onmicrosoft.com
# FDX_CLIENT_ID=your-client-id
# FDX_SCOPES=api://fd-agent-wallet-mcp/mcp:tools openid offline_access

# Override the MCP server URL (default: https://mcp.fd.xyz)
# Set this to point the CLI at a non-production environment.
# FDX_MCP_SERVER=https://...

# Override the token store path (default: ~/.fdx/auth.json)
# FDX_STORE_PATH=~/.fdx/auth.json

# Log file path (default: ~/.fdx/fdx.log)
# FDX_LOG_PATH=~/.fdx/fdx.log

# Log verbosity: debug | info | warn | error | off (default: info)
# FDX_LOG_LEVEL=info
